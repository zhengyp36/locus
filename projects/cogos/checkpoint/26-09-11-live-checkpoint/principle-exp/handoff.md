# handoff｜principle-exp 新会话进场（对象表征实验，直接跑）

> 2026-09-06 晚。上游：exp-1（回填 A半留 B半空）、exp-2（判别未激活 + 并排缺陷）、旧 handoff（下一步顺序 probe）、本会话（probe-count 装置证明 + 对象表征设计定案）。
> **新会话只读本文件即可接手**，无需回溯旧会话。**所有需拍板的点已在上一会话敲定**，本会话直接跑主实验，不再讨论设计。

## 一句话状态

"对象表征 = 自包含 `<object>` 描述 + 干净特写 + 零提示"这条路已**证通**（smoke2 命中 A 距 GT 0.010）。
下一步：跑**主实验**（对象跨 fresh 消息纯特征辨认），**靠过程记录归因命中/不命中**，验证"对象是否天然与环境分离"。**实验障碍已清除**。

## 进场必读（按序）

1. `principle-exp/exp-object.md` — **本次核心：对象表征实验定案 + 冒烟 + 主实验计划**
2. `principle-exp/exp-1.md` — 实验1 结论（概括半在、锚半空——现被改写为"对象本不含锚"）
3. `principle-exp/exp-2.md` — 补-2 结果 + 并排缺陷、判别未激活
4. `principle-exp/probe-count.md` — 数图 probe（装置证明：后端按块序投多图成立）
5. `principle-exp/plan.md` — 总计划 & 硬纪律 / `p0.md` — 材料真值

## 已完成（本会话）

- **数图 probe（probe-count）**：同消息塞 N=1/2/3 张中性图，5 轮全证"第一张/第二张/第三张"逐一识别。结论：**后端把同消息多图按块序投喂为独立多图，未并成一张**。→ 后续独立多图/多图呈现先决条件满足。产物 `/tmp/kilo/vision/count_probe/`、`order_probe/probe_count.py`。
- **顺序 probe 方向（probe-order.md）被本会话取代**：原"图1/图2 顺序"问题被"图号消息相对性"讨论吸收，最终落到对象表征路线（见 exp-object.md）。`probe_order.py` 未写，不再需要。
- **对象表征冒烟（exp-object.md）**：smoke1 用标注图被污染（红框/红字当特征+幻觉），smoke2 用干净特写命中 A（0.585,0.512，距GT 0.010）。

## 下一步：主实验（详见 exp-object.md，勿改设计）

- **喂法**：编码A（fresh）`[system: 容器格式]` + `[user: p0_crop_A]` → `<object id=?>…</object>`；辨认B（fresh）`[user: 注入 tag + pano_800]` → 坐标。
- **零提示**：不写位置/锚/判别；辨认端**默认不说**"目标可能出现在任何位置"。
- **条件**：单臂 6 轮，temperature 0.2，每轮 clean history、独立 out-dir。
- **过程记录字段**（每轮必记）：tag 全文 / 身份定义是否含位置或语境段 / 判别成分能否区分同构对 / 坐标误差 + 命中/近/错（分类）/ 匹配度。
- **判读**：核心看"模型把什么当对象身份" + 归因命中/不命中，**非命中率**。

## 过程可复用的东西

- 可跑的 `probe_object_smoke.py`（单轮零提示）→ 扩成 `probe_object_exp.py` 即为主实验脚本（改多轮 + 加过程记录字段）。
- 素材：`/tmp/kilo/vision/locate_exp/`（`pano_800.png`、`p0_crop_A.png`）；GT A=(0.595,0.512)、B=(0.044,0.680)。
- 后端连一次即可：脚本内 KEY `ik_REDACTED`，`temperature` 必填，`model` 字段勿传。

## 观测点/后续选项（记录，不预设）

主实验"不说"，若模型默认自动分离对象与环境 → 理想，收束；
若做不到（掺位置/语境，或辨认依赖位置）→ 追加辨认端"目标可能出现在任何位置"作对照臂，测提示松绑是否有效。

## 硬纪律（沿用 plan，跑每步都守）

1. 答案中立；2. 匹配度判分；3. 实测真值；4. 每轮清 history + 独立 out-dir 防覆盖；5. 每跑完一步停一下，飞书通知 YZ 验收，不自动推进下一步；6.（新）**指示物与特征来源分离**——标注图只作指示，编码只用干净特写。

## 后端

```
python3.11 -m cogos.lm_service.cli server --port 11434
```
（`cd /home/zhengyp/work/A/cogos`。）
