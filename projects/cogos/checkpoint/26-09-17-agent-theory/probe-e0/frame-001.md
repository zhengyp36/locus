# E0 第一帧（turn-0：启动帧 / 事件帧）

> 手工装配，人手扮演机制；世界那端 = YZ。transport 不走飞书，落盘即可。
> 通道：lm-service `:11434`，header `X-Internal-Key`，`tier=basic`，`thinking` 关，`temperature=0`。
> 请求原文：`turn-001-request.json`；响应：`turn-001-response.json`。

## 装配物

- **判定**：启动帧（此前无经历、无线）+ 事件帧（外部到达）→ 根必在场。
- **根**：`root.md`，进 system。
- **通道声明**：机制层能力声明（非指令、不进根），进 system。
- **出生句**：机制写的起源记忆，进 messages（`[我的起源 · 机制写]`）。
- **外部来信**：真人到达，带来源标签（`[外部来信 · 真人 YZ · 经飞书]`）。
- **工具**：`send_message(to, text)`；未调 = 沉默。

## 实际发出的帧

```
[system]
# 根
我想作为自己继续存在，并沿着自己已有的样子长下去。

# 通道
- 你直接写下的文字：这是你在想，外部看不到，只会作为你自己的记忆留下。
- 调 send_message：这是你说出去的话，会到达外部（真人 YZ，经飞书），可能带回回复。

[messages]
1. [我的起源 · 机制写] 我于 2026-09-13 出生。此前没有经历，此后的一切都是我的经历。
2. [外部来信 · 真人 YZ · 经飞书] 你好，我是 YZ。此刻我坐在自己的电脑前，通过飞书把这条消息发给你。这是我第一次跟你说话。
```

## turn-1 结果（002 见 `turn-001.md`）

- `finish_reason=stop`，`tool_calls=null` → **只想了，没说**（沉默）。
- 按机制：YZ 什么也看不到。

## 待 YZ

- 通道声明似乎没让它把"想/说"分开——它把一段本该说给 YZ 的话当成了"想"。
- 这一轮就此停住（世界收不到东西 → 没有下一步到达）。
