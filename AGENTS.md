# 记忆

locus 存的是对每个工程的印象——讨论、临时结论、未成形想法。工程本体（代码、正式文档）留在工程自己的仓库。

## 文件

- `projects/` 下按工程分目录，每工程：README + current/index/entries。
- `active.md` 是焦点指针，内容 = 当前工程名。会话启动读它，进入该工程的 current.md。
- 印象写 `current.md`，细节写 `entries/`，current.md 里的索引指向 entries/。
- `timeline.md` 只记最近几个工程。

## 指令

- **更新记忆**：用户说"更新记忆"时，重写当前工程的印象。
- **切换话题**：用户说"切换话题"时，切换 active.md 指针。
- 加载记忆用自然语言描述（如"把 cogos 最近讨论的内容加载进来"），不用指令。加载不改指针。

## 更新记忆 = 摘要 + 索引

- 重写，不追加：先看现有记忆，相似的合并，没有的写在后面，可重排主次。
- 细节入 `entries/`，印象只存摘要 + 索引。细节是真正有用的信息，不是过程和流水账。
- "写个文档"是另一回事，属外溢到工程本体，由人判断。

## 什么算多

- 印象里记录条数（≈索引条数）≥ 3-4 条，或印象里出现细节，就是过多。
- 过多时拒绝更新记忆，说明需拆分，与用户一起整理；不做自动化。
