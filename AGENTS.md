# 思考与回答

- 凭直觉回答，内容精简，别想太多，不怕出错。思考过程要短。
- 思考语言 = 输出语言，不刻意切换。
- 动作后总结用清单式：改的文件（各一行）、验证结果（一行）、遗留（一行），不复述 diff。
- edit 用最小唯一锚点：oldString 只贴"改动行 + 前后各一行上下文"，够唯一即可；大改动拆成多个内聚小步。
- 机器向产物用英文：代码、提交信息、变量名、日志、文件路径、结构化摘要。

# 记忆

locus 存的是对每个工程的印象——讨论、临时结论、未成形想法。工程本体（代码、正式文档）留在工程自己的仓库。

## 加载原则

- 最小化加载：会话启动只读 `active.md` -> 当前工程 `current.md`，不预读全量。
- 按需展开：需要细节时再读 `index.md` / `entries/`；缺了再补，宁少勿多。

## 文件

- `projects/` 下按工程分目录，每工程分两层：
  - 工程管理层（大写、确定）：`README.md`（工程是什么、remote URL + 本地本体路径）、`CHANGELOG.md`（阶段/变更记录）、`ROADMAP.md`（可选，下一阶段方向）、`ISSUES.md`（可选，遗留问题）。
  - 记忆层（小写、就地改、可乱）：`current.md`（当前印象）、`index.md`（索引）、`entries/`（细节）。
- `docs/`（可选）是本体文档，仅当工程本体落在 locus 内时才有；本体在外部的工程无此目录。
- `active.md` 是焦点指针，内容 = 当前工程名。会话启动读它，进入该工程的 current.md。
- `timeline.md` 只记最近几个工程。
- `tools/` 是 locus 自身工作流的工具（不属于被记忆工程本体），用法见 `tools/README.md`。

## 工程管理 vs 记忆

- 工程管理层是确定的锚，记忆乱了可从它拉回；避免追加成流水账，唯 `CHANGELOG.md` 追加、不重写。
- 记忆层是类人印象：可乱、可模糊，就地改，只动需要动的地方。
- 命名约定（子工程内）：大写 = 工程管理（确定），小写 = 记忆（模糊）；顶层指针/草稿（active、timeline、scratch）不套此约定。

## 更新记忆

- 就地改：看现有记忆，相似的合并，没有的补在后面，可重排主次；不用读全量、不做全量重排。
- 印象只存摘要 + 索引（指向 entries/），细节入 `entries/`。一刀切：**超过一句话的内容必须进 entries/**。
- 感觉写不下就拆到 `entries/`，不用数条目。
- "写个文档"是另一回事，属外溢到工程本体，由人判断。

# 指令

- **更新记忆**：用户说"更新记忆"时，就地更新当前工程的印象。
- **切换话题**：用户说"切换话题"时，切换 active.md 指针。
- 加载记忆用自然语言描述（如"把 cogos 最近讨论的内容加载进来"），不用指令。加载不改指针。
- **通知人**：做完阶段性成果、或遇到需人介入的问题，用 `tools/feishu_notify.py "<文本>"` 飞书通知（默认 YZ），然后停下等人。
