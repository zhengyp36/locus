# ENTRY · 规则整理（恢复入口）

> 用途：`/undo` 后从本文件读回上下文。YZ 撤销后会说"读入口"。
> 状态：**进行中**（2026-09-26）。本轮 = **快速整理规则**（非大重构、无大目标）。

## 一句话

规则有点乱，做**整理**即可：只求"不乱"，不追大目标。

**"乱"的定义（可检）**
1. **一处一说** — 同一件事只在一个文件定义，别处只引用。
2. **不打架** — 规则之间无矛盾。
3. **找得到** — 有索引进口 + AGENTS.md 有指针。

达到这三条 = 整理完。

## 怎么做（有界）

1. **层已定，不重想**：`AGENTS.md`（跨任务纪律，常驻）／`rules/`（任务方法，按名加载）／根 `README.md`（是什么/为什么，给人看）／`tools/`（环境事实＝数据）。
2. **去重**：砍重复。
3. **消矛盾**：按已定结论统一。
4. **归位**：任务规则→`rules/`；环境事实→`tools/`；结构说明→`README.md`。
5. **加索引 + 指针**：`rules/README.md` + AGENTS.md 一行。

"减法"是这套的**副产品**，不必单独立"目标"。

## 要处理的具体项

- **重复**：AGENTS.md 的 `# 记忆` 章 ↔ 根 `README.md`（README 已写全目录／每个工程／边界）。
- **矛盾**：
  - AGENTS.md `通知人…然后停下等人` vs `rules-revision.md §一`（通知 ≠ 停）。
  - AGENTS.md `不主动推进下一步` vs `§三`（目标内可推进）。
- **喂倾向**：AGENTS.md `别想太多，不怕出错` → 删（会被读成"立即动手"的授权）。
- **已定删**：AGENTS.md `edit 用最小唯一锚点…`（`§五`）。
- **收尾时**：补 `rules/` 指针 + 红线（secrets／force-push／发布／共享主分支）。

## 范围

`AGENTS.md` + `rules/` + 根 `README.md` + 近期工程留下的任务规则（如
`projects/cogos/checkpoint/26-09-26-screenlab/screenlab-rules.md`）。

⚠️ 任务规则里混着**环境事实**（IP／账户／坐标／helper）→ 剥到 `tools/`，不进规则。
⚠️ 任务规则里可能混着**跨任务的**（如 10min 闹钟、ctx 阈值交接）→ 归 `rules/` 的交接规则，不是某工程的。

## 读什么

1. 本文件。
2. `rules-revision.md` — §一~八 已定结论（消矛盾的依据）＋待定 A/B/C。
3. 被整理对象：`AGENTS.md`、根 `README.md`、`rules/`、`screenlab-rules.md`。

## 挂起（本次不做）

- 模型倾向线（"判断主导"：敢说能说／不立即动手／该动手时动手／不听指令）——**另属一事**。
  草稿 `rule-judgment-lead.md`，本次不读、不做。

## 执行前提

`/undo` 只回退**消息**、不回退**文件**：本文件与 `rules/` 撤销后仍在。
