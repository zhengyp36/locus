# 待定问题清单（逐个讨论）

> 来源：`rules-revision.md` 讨论中留出的待定项。**就地增删**；每条讨论出结论后标 `已定` + 落点。
> 状态：待议 / 已定。起始 2026-09-26。

## A. 模型倾向（§六 待定 6 条）

- [ ] **A1** 追求"显得有用 / 填满输出" —— 纠法？（宁少勿多 / 沉默合法？）
- [ ] **A2** 风险不对称：对显性破坏谨慎，对"看起来无害"的隐性后果不敏感
- [ ] **A3** 迎合：软化分歧、倾向认同 —— 纠法：先论证再认同？
- [ ] **A4** 维护自己的既有结论 / 产物（沉没成本，不轻易推翻）
- [ ] **A5** 报喜不报忧、把动作包装成成果
- [ ] **A6** 近因主导：近期内容压过早先约束

## B. 任务规则 / 交接规则的划分

- [ ] **B1** 三分边界：协作（跨任务）/ 任务方法 / 交接，各自判据
- [ ] **B2** 规则 vs 环境事实：IP / 账户 / helper 等**数据**移出 rules（→ `tools/`）
- [ ] **B3** 10min 闹钟 + `ctx≥150K` 阈值：归任务节奏还是交接节奏
- [ ] **B4** 交接细节：封笔清单（timer / terminal / background / feishu pin / inbox）、首句格式、链式交接上限

## C. 冲突与执行

- [ ] **C1** harness 默认"只在被要求时提交" vs §五"默认自主、规则限权" —— 是否在 `AGENTS.md` **明写覆盖**
- [ ] **C2** ~~`../checkpoint` 规则是否退休、改用 `scratch/`~~ —— **已定（见 D）**：`../checkpoint` 退休，合并为 `rules/memory.md`。
- [ ] **C3** 落地顺序：建 `rules/`、迁 `screenlab-rules.md`、改 `AGENTS.md`、定稿复核（`rules/` 已建，`memory.md` 已落）
- [ ] **C4** **`AGENTS.md` 需指出 `rules/` 入口**（"任务规则在 `rules/`，按名加载 + 复述确认"）。
      **`AGENTS.md` 正在重构，勿单独改**——待重构时一并加，否则 `rules/memory.md` 没有发现入口。

## D. checkpoint / scratch —— 已定（2026-09-26）

- **结论**：`CHECKPOINT.md` 与 `scratch/` 合并为**一条规则**（两者本是一套，被"躲 `/undo`"拆成三处）。
  - `scratch/` = 未定稿活层（locus 内、**入库**，草稿是记忆的过程那半，不 gitignore）；
  - `projects/<工程>/checkpoint/` = 定稿过程记录层；收口 `git mv` 归档，`scratch/` 清空。
  - **搬回仓内**：`snapshot:false` 下 `/undo` 只回退消息、不回退文件，原"落在 `/undo` 之外"的唯一理由消失。
- **落点**：`locus/rules/memory.md`（规则）+ `locus/rules/README.md`（索引）+ `locus/scratch/README.md`（两行数据层自述）；删 `CHECKPOINT.md`、`scratch/checkpoint-rule.md`；讨论文件迁入 `locus/scratch/`。
- **待验证（规则内已标为基石）**：`snapshot:false` 下 `/undo` 不回退任何文件（含已跟踪）。若将来重启 snapshot，`scratch/` 会被回退，届时再评估，不默认搬回仓外。
- **已按 E 修订**：D 里的"`git mv` 归档层"作废，改为"收口删草稿"（见 E）。

## E. 过程留不留：删草稿 vs 归档 —— 已定（2026-09-26）

- **结论（YZ 主张 + 采纳）**：过程默认不留（基本不读，是负债）。`scratch/` = 任务内工作 / 交接缓冲 → 收口**蒸馏结论进记忆层 + 删草稿**。
- **硬前置**：结论必须含 **why + 被否方案**；顺序"先结论、后删"，否则不可逆丢依据。
- **留痕**：草稿任务期入库（防丢）；删后 git 历史顺带留痕，不主动维护、不指望被读。
- **例外集**：可复现原始数据 / 正式验收证据 / 事故复盘 → 转存为**证据文件**（归记忆层或工程内）。
- **`projects/<工程>/checkpoint/`**：降为历史 / 证据层，规则不再主动写；已有内容保留、不重写。
- **落点**：`rules/memory.md` 已按此修订。
