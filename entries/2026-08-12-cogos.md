# CogOS 讨论（2026-08-12）

## 摘要

交接确认 → CLI 集成测试 → 真实账号集成测试。讨论了版本依赖声明和 bot_type 补齐，修复了 invite-bot 的 API 调用 bug，补齐了 --help 命令列表。

## 关键结论

1. **交接完成** — inbox 内容已消化，状态从"待交接"转为"待调试验证和下一阶段设计"
2. **版本依赖声明** — 项目根目录 `.python-version` + `pyproject.toml` `requires-python = ">=3.11"`，解决 AI 先尝试错误版本的问题
3. **bot_type 补齐** — 8 个 bot 账号 JSON 手动添加 `"bot_type": "test"`，设计决策是不增加命令
4. **测试范围** — 逐层验证：bot 管理 → 群聊管理 → speak → watch-bot，后三步有外部副作用需控制范围

## CLI 集成测试（已完成，340 pass）

`tests/feishu/test_cli_integration.py`（10 个测试），同进程启动 daemon（mock WSManager），通过 Unix socket 验证协议。

## 真实账号集成测试（进行中）

### 已验证通过
- daemon 启动（manual 模式），陈梦瑶 bot WS 连接飞书 ✓
- `create-group` 建群 + YZ 入群 ✓
- `invite-members --id-type app_id` 拉 agi-bot ✓
- `speak` 发消息 ✓
- `add-bot` / `list-bot` 管理连接 ✓

### 修复
- **`core.py` `add_members` bug**：`member_id_type` 应作为 query param（`params=`）而非 JSON body（`json=`）。修复前传 `app_id` 报 99992351。

### 待解决
- WS 已连接但事件未到达 daemon日志，需排查 `register_p2_customized_event` 事件路由

### 改进
- `commands.py` `DESCRIPTION` 补齐全部 23 个命令
- CogOS 工程创建了 `AGENTS.md`（约定：新增命令需更新 DESCRIPTION）
- locus 新增通用机制：`projects.md` `约定:` 字段 → 自动加载工程约定文件

## 下一步

- 排查 WS 事件接收问题
- 事件通后确定方向：最小闭环验证 / 认知树设计 / agent 运行时设计
